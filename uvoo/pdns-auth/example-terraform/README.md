# This is simple example of using Terraform Provider for PowerDNS

```
cp example.env .env
cp example.env.secrets .env.secrets
```

Make your variable changes in .env and .env.secrets then run

```
. .env
. .env.secrets
./main.sh
```

